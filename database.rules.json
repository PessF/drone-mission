{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      "$room": {
        ".read": "$room.matches(/^[0-9]{4}$/)",
        ".write": "$room.matches(/^[0-9]{4}$/)"
      }
    }
  }
}
